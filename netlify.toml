[build]
  functions = "functions"  # Directory where your serverless functions are located
  publish = "."            # Publish directory (can be root if no static assets)

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"  # Redirect all requests to the serverless function
  status = 200
